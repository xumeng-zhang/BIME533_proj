# NHANES Oral Health Dashboard - Streamlit Cloud Requirements
# Optimized for fast deployment (removed heavy dependencies not needed at runtime)

# Core data handling
pandas==2.2.3
numpy==1.26.4

# Dashboard framework
streamlit==1.39.0

# Interactive visualizations (only package needed for charts)
plotly==5.24.1

# Utilities
python-dateutil==2.9.0
